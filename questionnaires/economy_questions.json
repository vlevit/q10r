
{
    "title": "Economy questions",
    "questions": [
        {
            "type": "string",
            "label": "Your Name",
            "required": true
        },
        {
            "type": "checkbox",
            "label": "Should we introduce a carbon tax ?",
            "required": true,
            "options": [
                "yes",
                "no",
            ]
        },
    ]
}
